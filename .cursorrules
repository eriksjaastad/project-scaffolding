# Cursor Rules - Project Scaffolding (Meta-Project)

> **IMPORTANT:** Follow role definitions and workflow in `/docs/AGENTS.md` (or symlinked `AGENTS.md` in project root).

---

## 🎯 What This Project Is

Meta-project for standardizing ecosystem structures, AI-agent role definitions, and multi-model review protocols.

**Domain:** Meta / Automation / Governance
**Status:** Active Development

---

## 🛠 Tech Stack

- **Language:** Python 3.11+
- **Frameworks:** Click, Pydantic, pytest, tenacity, rich
- **Database:** None (Local-first file system)
- **Deployment:** Local CLI

---

## 📋 Definition of Done (Project-Specific)

- [x] Code follows project coding standards (see below)
- [x] Changes logged to `WARDEN_LOG.yaml`
- [x] Documentation updated (if user-facing changes)
- [x] Tests pass: `pytest tests/`

---

## 🚀 Execution Commands

```bash
# Environment
source venv/bin/activate

# Run tests
pytest tests/

# Run main script
python scaffold_cli.py --help
```

---

## 📁 Project Structure

```
project-scaffolding/
├── scaffold/        ← Core logic (review, cli, utils)
├── scripts/         ← Automation scripts
├── templates/       ← Standardized file templates
└── Documents/       ← Project documentation
```

---

## 🚫 Context Window Management

**NEVER waste context on:**
- `node_modules/`, `__pycache__/`, `.git/`
- `venv/` — Virtual environment
- `_cursorrules_backups/` — Backup files

**When reading files:**
- Use targeted reads for large files
- Prefer `codebase_search` for finding patterns

---

## 📜 Coding Standards (Project-Specific)

- **File Ops:** Use `pathlib.Path`
- **Data:** Use YAML for structured data (SSOT pattern)
- **Library Code:** No prints, use `logging`
- **AI Ethics:** Follow hierarchy in `AGENTS.md`

---

## ⚠️ Project-Specific Constraints

- NEVER use `rm`. ALWAYS use `send2trash` to move files to the system Trash.
- ALWAYS verify `venv` is active before running tests
- MANDATE "Local-First" prompting for worker tasks

---

## 🔗 Related Files

- **Ecosystem Constitution:** `AGENTS.md`
- **Review Protocol:** `REVIEWS_AND_GOVERNANCE_PROTOCOL.md`
- **Project Index:** `[[00_Index_project-scaffolding]]`
- **Learnings:** `Documents/reference/LOCAL_MODEL_LEARNINGS.md`

---

## 📚 Source Context (Optional)

If this project builds on patterns from other projects, document them here:

| Source Project | Pattern Borrowed |
|----------------|------------------|
| image-workflow | FileTracker pattern |
| Trading Projects | SSOT via YAML |

---

*Project-specific rules. See AGENTS.md for universal standards.*



