# PROJECT SPEC: {{PROJECT_NAME}}
**Status:** Draft / Approved / In-Audit
**Conductor:** Erik Sjaastad
**Architect:** Claude Code CLI
**Floor Manager:** [Antigravity / Gemini]
**Workers:** [Local Ollama models / Sub-agents — depends on computer]

---

## 1. THE "NORTH STAR" (Objective)
*What is the simplest version of this idea? Why are we building it?*
> [Example: A portable, local-first trading dashboard that eliminates ENV-leak risk.]

## 2. CORE REQUIREMENTS (The "Must-Haves")
*If these aren't met, the project is a failure.*
- [ ] **Infrastructure:** Secrets in `.env` file (Doppler is at capacity - 10/10 projects).
- [ ] **Portability:** Zero absolute paths. Must run from any directory via `PROJECTS_ROOT` anchor.
- [ ] **Reliability:** No silent failures (`except: pass` is a critical rejection).
- [ ] **Context Optimized:** Must be Repo-Mix compatible for "Stranger's Reviews" (Claude Web).

## 3. DATA ARCHITECTURE (The Shape of Things)
*Describe the 'Source of Truth' for data.*
- **Secrets:** Local `.env` file (loaded via `python-dotenv`). See `DOPPLER_QUICK_REF.md` for legacy projects.
- **Reasoning:** Local-First (Ollama: DeepSeek-R1) for code logic.
- **Storage:** [e.g., Local NDJSON / YAML / SQLite].

## 4. EXTERNAL DEPENDENCIES MANIFEST
*Every third-party service, account, and secret this project needs. Fill this out BEFORE handing off to Kiro or any agent. No surprises mid-build.*

**Status:** [ ] All accounts created  [ ] All secrets in Doppler  [ ] Ready for production switch

| Dependency | Type | Account Status | Secret in Doppler? | Owner |
|------------|------|---------------|-------------------|-------|
| *e.g., Stripe* | *Payment API* | *Exists (test mode)* | *Yes: STRIPE_SECRET_KEY* | *Erik* |
| *e.g., Resend* | *Email service* | *Needs signup* | *No — blocked until account created* | *Erik* |

**Accounts Erik Needs to Create:**
- [ ] *(list each service requiring manual signup)*

**Mock-to-Production Cutover:**
- Mock data is used until: *(define the trigger — e.g., "all Doppler secrets populated")*
- Agent switches to production when: *(explicit instruction, not implicit assumption)*
- **Rule:** No agent should assume production readiness. Erik confirms via Doppler population + explicit go-ahead.

**Already Exists (from EXTERNAL_RESOURCES.yaml):**
- *(list services this project reuses from existing infrastructure)*

---

## 5. EXECUTION & CLI LOGIC
*Standard ecosystem entry points (Replacing npm/web bias).*
1. **Bootstrap:** `source venv/bin/activate`
2. **Secrets Injection:** Loaded via `load_dotenv()` (or `doppler run --` for legacy projects)
3. **Primary Run:** `python scripts/[main_script].py`
4. **Validation:** `pytest` (Standard 100% pass requirement).

## 6. GOVERNANCE & AUDIT RULES
*Rules for the Code Review Agents (The "Ruthless Auditor" layer).*
- **Reject if:** Hardcoded machine-specific paths (e.g., `[USER_HOME]/...`).
- **Reject if:** API keys or secrets in plain text or code comments.
- **Reject if:** Function length > 40 lines without architectural justification.
- **Enforce:** `CODE_QUALITY_STANDARDS.md` and `AGENTS.md` protocols.

## 7. TECHNICAL DEBT & "KNOWNS"
*What are we ignoring for now?*
- [ ] UI is "functional-minimal" (Industrial over Aesthetic).
- [ ] Scaling guards for >1GB datasets not yet implemented.
- [ ] Documentation is Spec-first; full README comes later.

## Related Documentation

**Project-Specific References:**
- Add links to project-specific architecture, patterns, and guides here

**Universal Resources:**
- [Code Review Anti-Patterns](.agent/rules/code-review-anti-patterns.md)
